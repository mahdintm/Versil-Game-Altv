 var okusername = false;
 var okpassword = false;
 var okrepassword = false;
 var okemail = false;
 var okmobile = false;
 var okbrithday = false;
 
 
//---------------------------------------------------------------------------------------------------------------------
           /* function showFunction(action) {
                if (action == "login") {
                    var userNametext = document.getElementById("usernameText").value
                    var passwordText = document.getElementById("passwordText").value
                    mta.triggerEvent("loginPlayer", userNametext, passwordText)
                    
                } else if (action == "register") {
                    if(okusername == true)
                    {
                        if(okpassword == true)
                        {
                            if(okrepassword == true)
                            {
                                if(okemail == true)
                                {
                                   // if(okmobile == true)
                                   // {
                                       // if(okbrithday == true)
                                       // {
                                            var userNametext = document.getElementById("usernametext").value
                                            var passwordText = document.getElementById("passwordtext").value
                                            var password2Text = document.getElementById("password2").value
                                            var emailText = document.getElementById("email").value
                                            mta.triggerEvent("registerPlayer", userNametext, passwordText, password2Text, emailText)
                                       // }
                                   // }
                                }
                            }
                        }
                    }	
                } else if (action == "recover") {
                    var userNametext = document.getElementById("uname").value
                    mta.triggerEvent("recoverPlayer", userNametext)	
                }
            };*/